apply plugin: 'com.android.application'

android {
    compileSdkVersion 26
    buildToolsVersion "29.0.2"

    defaultConfig {
        applicationId "com.hty.LocusMapUCMap"
        minSdkVersion 24
        targetSdkVersion 26
    }
}

dependencies {
    compile files('libs/UCMapForOpenGIS.jar')
}